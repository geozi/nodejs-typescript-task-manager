/**
 * Role enum.
 * @module src/domain/enums/role.enum
 */

/**
 * Enum for user roles.
 *
 * @readonly
 * @enum {string}
 */
export enum Role {
  /**
   * Represents the 'admin' role.
   * @type {string}
   */
  Admin = "Admin",

  /**
   * Represents the 'general' role.
   * @type {string}
   */
  General = "General",
}
